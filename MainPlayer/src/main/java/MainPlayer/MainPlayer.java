package MainPlayer;

public class MainPlayer {

	/**
	 * used only for launching
	 * @param args the command line arguments
	 */
	public static void main(String[] args) {
		MainPlayerUI ui = new MainPlayerUI();

		ui.main(args);
	}
}
